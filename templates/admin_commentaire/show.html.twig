{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ commentaire.contenu }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ commentaire.createdAt ? commentaire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

   <button class="btn btn-primary"><a href="{{ path('app_admin_commentaire_index') }}">Retour à la liste</a></button>

     <button class="btn btn-secondary"><a href="{{ path('app_admin_commentaire_edit', {'id': commentaire.id}) }}">Modifer</a></button>

    {{ include('admin_commentaire/_delete_form.html.twig') }}
{% endblock %}
