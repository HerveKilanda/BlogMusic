{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article.titre }}</td>
            </tr>
            <tr>
                <th>Image</th>
              <td><img src="{{ article.image }}" alt"image"></td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ article.contenu }}</td>
            </tr>
        </tbody>
    </table> 

    <button type="button" class="btn btn-danger"><a href="{{ path('app_admin_article_index') }}">Retour à la liste des articles</a></button>
    <br>

     <button type="button" class="btn btn-danger mt-3"><a href="{{ path('app_admin_article_edit', {'id': article.id}) }}">Modifier</a></button>

    {{ include('admin_article/_delete_form.html.twig') }}
{% endblock %}



    